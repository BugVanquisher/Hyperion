apiVersion: apps/v1
kind: Deployment
metadata:
  name: mms-redis
  namespace: mms
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mms-redis
  template:
    metadata:
      labels:
        app: mms-redis
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          ports:
            - containerPort: 6379
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "200m"
              memory: "256Mi"
